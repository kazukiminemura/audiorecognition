Deployment (PyInstaller)

Build the desktop app into a single-folder distribution using PyInstaller.

Prerequisites
- Windows
- `venv` created at the repo root

Build
1) Open PowerShell in the repo root.
2) Run:

```powershell
.\deployment\build.ps1
```

Output
- The app is created at `dist\audiorecognition\`.
